# Runtime dependencies

The visualization uses only browser-native HTML, CSS, SVG, and JavaScript. It
does not load third-party runtime libraries, fonts, analytics, or hosted assets.

Repository licensing is defined by the root `LICENSE` file.
